# designPatterns

https://refactoring.guru

1.Singleton — «один объект на всю систему»

2.Factory Method — «подкласс решает, что создавать»

3.Abstract Factory — «создать совместимый набор объектов»

4.Builder — «собирать объект шаг за шагом»

5.Adapter — «сделать несовместимое совместимым»

6.Decorator — «добавить поведение, не меняя класс»

7.Facade — “одна кнопка” для запуска сложного процесса.

8.Strategy — «менять алгоритм во время выполнения»

9.Template Method — «алгоритм общий, шаги разные»

10.Iterator — «проходить по элементам коллекции, не раскрывая её внутреннюю структуру»

1️⃣ Singleton

Intent: гарантировать существование одного экземпляра класса.
Problem: множественные экземпляры объекта приводят к рассинхронизации состояния.
Solution: скрыть конструктор и предоставить единую точку доступа.
Result: один инстанс, общее состояние.

2️⃣ Factory Method

Intent: определить интерфейс создания объекта, позволяя подклассам выбирать конкретный класс.
Problem: код зависит от конкретных классов (new).
Solution: вынести создание в переопределяемый метод.
Result: ослабленная связность, расширяемость.

3️⃣ Abstract Factory

Intent: предоставить интерфейс для создания семейств связанных объектов.
Problem: объекты должны быть совместимы между собой.
Solution: каждая фабрика создаёт согласованный набор.
Result: гарантия совместимости продуктов.

4️⃣ Builder

Intent: отделить процесс построения сложного объекта от его представления.
Problem: сложный объект имеет много конфигураций.
Solution: пошаговая сборка через билдера.
Result: читаемое и гибкое создание объектов.

5️⃣ Adapter

Intent: преобразовать интерфейс одного класса в интерфейс, ожидаемый клиентом.
Problem: несовместимые интерфейсы не могут работать вместе.
Solution: обернуть объект адаптером.
Result: совместимость без изменения существующего кода.

6️⃣ Decorator

Intent: динамически добавлять объектам новое поведение.
Problem: наследование приводит к взрыву классов.
Solution: оборачивать объект декораторами.
Result: гибкое расширение поведения.

7️⃣ Facade

Intent: предоставить упрощённый интерфейс к сложной подсистеме.
Problem: клиент зависит от множества классов.
Solution: фасад делегирует вызовы подсистемам.
Result: упрощённое использование и низкая связность.

8️⃣ Strategy

Intent: определить семейство алгоритмов и сделать их взаимозаменяемыми.
Problem: логика алгоритмов зашита в if/else.
Solution: вынести алгоритмы в отдельные классы.
Result: смена поведения во время выполнения.

9️⃣ Template Method

Intent: определить скелет алгоритма, позволяя подклассам переопределять шаги.
Problem: алгоритм повторяется с небольшими отличиями.
Solution: базовый класс фиксирует порядок, подклассы — детали.
Result: переиспользование алгоритма без дублирования.

🔟 Iterator

Intent: предоставить способ последовательного доступа к элементам коллекции.
Problem: клиенту не должно быть известно внутреннее устройство коллекции.
Solution: вынести логику обхода в итератор.
Result: единый и безопасный способ перебора.
